# This repo contains a simple RNN model to learn a character level language model. 

The data is also provided with this repo. To run the model, just clone the repo and simply do

```python train.py```
